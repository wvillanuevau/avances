import React from "react";
import ReactDOM from "react-dom/client";
import ProjectTrackerApp from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ProjectTrackerApp />
  </React.StrictMode>
);
